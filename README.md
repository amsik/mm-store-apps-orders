# mm-store-apps-orders

Order management service with a strict order state machine (`OPEN → IN_PROGRESS → COMPLETE`), exposed through a GraphQL API.
Built with NestJS, Apollo, Prisma and MongoDB. The repo also contains a minimal web client.

> Work in progress. The implementation plan is in [`tasks/plan.md`](tasks/plan.md).
