# Copy to apps/web/.env for local overrides. Read at build time by Vite (baked into the bundle).

# GraphQL endpoint of the API. Its CORS_ORIGINS must include this app's origin.
VITE_API_URL=http://localhost:3000/graphql
